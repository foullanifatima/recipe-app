/**
 * ! todo testing ideas
 * ? try if the field is empty it should print nothing
 * ? test if the field is empty the button should be disabled
 * ? test if the field contain char the button should be enabled
 * ? test if  it is loading so the snipper should appear
 * ? test id it is not loading so the snipper should not appear
 */

/**
 * !Notes for completing tommorow tasks
 * i do have state and data in home i should take them to show all cartes
 * ? add a search form and style it
 * ? print all result
 * code go to specific data using id
 * style the single page
 * find a good way to join ingredients with measure
 * print video
 *
 */
